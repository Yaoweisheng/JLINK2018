<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jlink.dao.RoleMenuDao" >
  <resultMap id="BaseResultMap" type="com.jlink.entity.RoleMenu" >
    <result column="id" property="id" jdbcType="INTEGER" />
    <result column="menu_id" property="menuId" jdbcType="VARCHAR" />
    <result column="role_id" property="roleId" jdbcType="INTEGER" />
  </resultMap>
  <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="com.jlink.entity.RoleMenu" >
    insert into role_menu (menu_id, role_id)
    values (#{menuId,jdbcType=VARCHAR}, #{roleId,jdbcType=VARCHAR})
  </insert>
  <insert id="batchSave" useGeneratedKeys="true" keyProperty="id" >
    insert ignore into role_menu(menu_id, role_id)
    values
    <foreach collection="menuIds" item="mid" separator=",">
      (#{mid}, #{roleId})
    </foreach>
  </insert>
  <delete id="deleteByRoleId" parameterType="Integer">
    delete from role_menu where role_id = #{roleId}
  </delete>
  <select id="getMenusByRoleId" resultType="String" parameterType="Integer" >
    select menu_id
    from role_menu
    where role_id = #{roleId}
    order by menu_id
  </select>
</mapper>